---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: brrpolice
spec:
  components:
    - ../../../../components/volsync
    - ../../../../components/zeroscaler
  dependsOn:
    - name: rook-ceph-cluster
      namespace: rook-ceph
  interval: 1h
  path: ./kubernetes/apps/default/brrpolice/app
  patches:
    # Backups OFF (redo-able). Keeps the PVC, pauses the RS.
    - target:
        kind: ReplicationSource
      patch: |-
        - op: add
          path: /spec/paused
          value: true
  postBuild:
    substitute:
      APP: brrpolice
      VOLSYNC_SCHEDULE: "12 * * * *"
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: default
  wait: false
