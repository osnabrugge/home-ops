---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: recyclarr
spec:
  components:
    - ../../../../components/volsync
  dependsOn:
    - name: rook-ceph-cluster
      namespace: rook-ceph
  interval: 1h
  path: ./kubernetes/apps/default/recyclarr/app
  patches:
    # Backups OFF (config, redo-able). Keeps the PVC, pauses the RS.
    - target:
        kind: ReplicationSource
      patch: |-
        - op: add
          path: /spec/paused
          value: true
  postBuild:
    substitute:
      APP: recyclarr
      VOLSYNC_SCHEDULE: "38 * * * *"
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: default
  wait: false
