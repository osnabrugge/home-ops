---
# Stable Redis endpoint for Diode services — targets only the Dragonfly master to avoid read-only replica errors.
apiVersion: v1
kind: Service
metadata:
  name: dragonfly-diode
spec:
  selector:
    app: dragonfly
    app.kubernetes.io/name: dragonfly
    role: master
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
      protocol: TCP
