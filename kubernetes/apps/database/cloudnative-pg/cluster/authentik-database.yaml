---
# Authentik database — created in the existing postgres16 cluster.
# The authentik role is managed via spec.managed.roles in cluster16.yaml.
apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: authentik
spec:
  cluster:
    name: postgres16
  name: authentik
  owner: authentik
