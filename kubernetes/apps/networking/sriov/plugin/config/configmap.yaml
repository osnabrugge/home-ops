apiVersion: v1
kind: ConfigMap
metadata:
  name: sriovdp-config
  namespace: networking
data:
  config.json: |
    {
        "resourceList": [{
                "resourceName": "sriov_netdevice",
                "selectors": {
                    "vendors": ["8086"],
                    "devices": ["10ed"],
                    "drivers": ["ixgbevf"]
                }
            }
        ]
    }
