---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./grafanadashboard.yaml
  - ./httproutes.yaml
  - ./mcpgroup.yaml
  # TODO(sean): these four are referenced but not yet written. The mcp-gateway
  # HTTPRoute already points at the vmcp-mcp-gateway backend, so the gateway
  # hostname stays down until virtualmcpserver.yaml exists.
  # - ./externalsecret.yaml
  # - ./mcpoidcconfig.yaml
  # - ./mcptelemetry.yaml
  # - ./podmonitor.yaml
  # - ./virtualmcpserver.yaml
