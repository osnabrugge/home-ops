---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/toolhive.stacklok.dev/mcpserver_v1alpha1.json
apiVersion: toolhive.stacklok.dev/v1alpha1
kind: MCPServer
metadata:
  name: grafana-mcp
spec:
  podTemplateSpec:
    metadata:
      annotations:
        secret.reloader.stakater.com/reload: grafana-mcp-secret
  image: docker.io/grafana/mcp-grafana:1.2.0@sha256:cfae68f893dbe40cb6df69579f78beb226f973ded4436d47c844f91d2f00dc53
  transport: streamable-http
  proxyPort: 8080
  mcpPort: 8000
  secrets:
    - name: grafana-mcp-secret
      key: token
      targetEnvName: GRAFANA_SERVICE_ACCOUNT_TOKEN
  env:
    - name: GRAFANA_URL
      value: http://grafana-service.observability.svc.cluster.local:3000
  permissionProfile:
    type: builtin
    name: network
  args:
    - --transport
    - streamable-http
    - --address
    - 0.0.0.0:8000
    # ToolHive is the trusted reverse proxy and may rewrite the Host header.
    - --allowed-hosts
    - "*"
  resources:
    requests:
      cpu: 25m
      memory: 96Mi
    limits:
      memory: 384Mi
  groupRef:
    name: mcp-tools
