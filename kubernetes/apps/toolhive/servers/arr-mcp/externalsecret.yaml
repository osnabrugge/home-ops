# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: sonarr-mcp
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: azurekv
  target:
    name: sonarr-mcp-secret
    template:
      data:
        SONARR_API_KEY: "{{ .SONARR_API_KEY }}"
  dataFrom:
    - extract:
        key: sonarr
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: radarr-mcp
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: azurekv
  target:
    name: radarr-mcp-secret
    template:
      data:
        RADARR_API_KEY: "{{ .RADARR_API_KEY }}"
  dataFrom:
    - extract:
        key: radarr
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: prowlarr-mcp
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: azurekv
  target:
    name: prowlarr-mcp-secret
    template:
      data:
        PROWLARR_API_KEY: "{{ .PROWLARR_API_KEY }}"
  dataFrom:
    - extract:
        key: prowlarr