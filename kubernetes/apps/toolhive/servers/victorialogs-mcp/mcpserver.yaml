---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/toolhive.stacklok.dev/mcpserver_v1beta1.json
apiVersion: toolhive.stacklok.dev/v1beta1
kind: MCPServer
metadata:
  name: victorialogs-mcp
spec:
  image: ghcr.io/victoriametrics/mcp-victorialogs:v1.9.0@sha256:753bb4fbe402ca1a782842f44636830f9895736f11d7fe783b924f8a0daf083d
  transport: streamable-http
  mcpPort: 8081
  proxyPort: 8080
  permissionProfile:
    type: builtin
    name: network
  env:
    - name: VL_INSTANCE_ENTRYPOINT
      value: http://victoria-logs.observability.svc.cluster.local:9428
    - name: MCP_SERVER_MODE
      value: http
    - name: MCP_LISTEN_ADDR
      value: :8081
    - name: MCP_LOG_FORMAT
      value: json
  resources:
    requests:
      cpu: 50m
      # Ships an embedded vector DB for the `documentation` tool; that is the
      # bulk of the footprint. Add `documentation` to MCP_DISABLED_TOOLS to shrink.
      memory: 512Mi
    limits:
      memory: 1Gi
  groupRef:
    name: mcp-tools
