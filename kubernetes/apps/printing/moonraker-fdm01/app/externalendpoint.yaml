---
# Exposes fdm01's Moonraker API (192.168.42.25:7125) through the internal gateway
# at https://fdm01.homeops.ca with TLS + WebSocket support. This lets the browser
# UIs (mainsail/fluidd/pgcode), which are served over HTTPS, talk to Moonraker over
# HTTPS too — avoiding the mixed-content block you'd hit calling http://fdm01:7125
# directly. Implemented as a selector-less Service + manual EndpointSlice (pure
# Kubernetes/Gateway API, no Envoy-specific Backend CRD needed).
#
# NOTE (host-side, you apply on fdm01): add to moonraker.conf [authorization]:
#   cors_domains:
#     https://mainsail.homeops.ca
#     https://fluidd.homeops.ca
#     https://pgcode.homeops.ca
#   trusted_clients:
#     192.168.42.0/24
# and reload Moonraker. (printguard talks to Moonraker server-side, no CORS needed.)
apiVersion: v1
kind: Service
metadata:
  name: moonraker-fdm01
spec:
  type: ClusterIP
  ports:
    - name: moonraker
      port: 7125
      targetPort: 7125
      protocol: TCP
---
apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: moonraker-fdm01
  labels:
    kubernetes.io/service-name: moonraker-fdm01
addressType: IPv4
ports:
  - name: moonraker
    port: 7125
    protocol: TCP
endpoints:
  - addresses:
      - 192.168.42.25
    conditions:
      ready: true
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: moonraker-fdm01
spec:
  hostnames:
    - fdm01.homeops.ca
  parentRefs:
    - name: envoy-internal
      namespace: network
  rules:
    - backendRefs:
        - name: moonraker-fdm01
          port: 7125
---
# fdm01 (BTT Pad 7) crowsnest webcam (192.168.42.25:8080) exposed over HTTPS at
# https://cam-fdm01.homeops.ca so the browser UIs (mainsail/fluidd), served over
# HTTPS, can load the MJPEG stream/snapshot without the mixed-content block.
# PrintGuard connects to the camera server-side so it never needed this.
apiVersion: v1
kind: Service
metadata:
  name: cam-fdm01
spec:
  type: ClusterIP
  ports:
    - name: webcam
      port: 8080
      targetPort: 8080
      protocol: TCP
---
apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: cam-fdm01
  labels:
    kubernetes.io/service-name: cam-fdm01
addressType: IPv4
ports:
  - name: webcam
    port: 8080
    protocol: TCP
endpoints:
  - addresses:
      - 192.168.42.25
    conditions:
      ready: true
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: cam-fdm01
spec:
  hostnames:
    - cam-fdm01.homeops.ca
  parentRefs:
    - name: envoy-internal
      namespace: network
  rules:
    - backendRefs:
        - name: cam-fdm01
          port: 8080
