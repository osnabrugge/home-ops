---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: weave-gitops
  namespace: flux-system
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: azurekv
  target:
    name: weave-gitops-secret
    creationPolicy: Owner
  data:
    - secretKey: adminPassword
      remoteRef:
        key: weave-admin-password
    # - secretKey: WEAVEGITOPS_OAUTH_CLIENT_SECRET
    #   remoteRef:
    #     key: weave-authelia
