.claude/
.copilot/
.private/
.pytest_cache/
.venv/
.vs/
*.crt
*.env
*.iso
*.key
.decrypted~*
.DS_Store
Thumbs.db
kubeconfig
talosconfig
# Rendered Talos machineconfigs contain secrets — never commit
talos/rendered/
.tmp-*
# Sensitive credential files
youtube-cookies.txt

# raw switch configs contain credentials; only sanitized copies are committed
docs/backups/*-running-config-*.txt
!docs/backups/*.sanitized.txt
