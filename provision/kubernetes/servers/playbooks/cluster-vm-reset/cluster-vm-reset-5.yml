---
- name: Cluster Reset
  hosts:
    - tiny05
  become: true
  gather_facts: true
  any_errors_fatal: true
  pre_tasks:
    - name: Pausing for 2 seconds...
      ansible.builtin.pause:
        seconds: 2
  tasks:

    - name: stop {{ vm.worker5_name }}
      command: "qm stop {{ vm.worker5_id }}"

    - name: destroy {{ vm.worker5_name }}
      command: "qm destroy {{ vm.worker5_id }}"
