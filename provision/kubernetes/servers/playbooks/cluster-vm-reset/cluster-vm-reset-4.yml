---
- name: Cluster Reset
  hosts:
    - tiny04
  become: true
  gather_facts: true
  any_errors_fatal: true
  pre_tasks:
    - name: Pausing for 2 seconds...
      ansible.builtin.pause:
        seconds: 2
  tasks:

    - name: stop {{ vm.worker4_name }}
      command: "qm stop {{ vm.worker4_id }}"

    - name: destroy {{ vm.worker4_name }}
      command: "qm destroy {{ vm.worker4_id }}"
