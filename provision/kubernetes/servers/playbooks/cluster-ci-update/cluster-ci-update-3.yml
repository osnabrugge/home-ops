---
- name: Update Cloud-Init Config
  hosts:
    - tiny03
  become: true
  gather_facts: true
  any_errors_fatal: true
  pre_tasks:
    - name: Pausing for 2 seconds...
      ansible.builtin.pause:
        seconds: 2
  tasks:

    - name: set {{ vm.master3_name }} cloud-init config
      command: "qm set {{ vm.master3_id }} --cicustom user=nfs-nas:snippets/{{ vm.master3_name }}-user.yml,network=nfs-nas:snippets/{{ vm.master3_name }}-network.yml,meta=nfs-nas:snippets/meta.yml"

    - name: set {{ vm.worker3_name }} cloud-init config
      command: "qm set {{ vm.worker3_id }} --cicustom user=nfs-nas:snippets/{{ vm.worker3_name }}-user.yml,network=nfs-nas:snippets/{{ vm.worker3_name }}-network.yml,meta=nfs-nas:snippets/meta.yml"
